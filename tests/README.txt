study project with smart contracts on the SOLANA blockchain developed in RUST, using the anchor.

The smart contract stores a list of GIFs on the blockchain and accepts new GIFs sent by users.

#run validator 
solana-test-validator


#run anchor test less validator
anchor test --skip-local-validator

